function main() {
    let a = 5;
    let b = 10;
    let max = a > b ? a : b;
    let min = a < b ? a : b;
    print(max);
    print(min);
    
    let x = -5;
    let abs = x < 0 ? 0 - x : x;
    print(abs);
    
    return 0;
}
